/**
 * xyzToCubeTextureCoord
 * Returns uv-coordinate of the corresponding xyz-coordinate on in 3d space
 */
function xyzToCubeTextureCoord(x, y, z) {
  return [0.0, 0.0];
}

/**
 * cubeTextureCoordToXyz
 * Returns xyz-coordinate of the corresponding uv-coordinate on a cube map
 */
function cubeTextureCoordToXyz(u, v) {
  return [0.0, 0.0, 0.0];
}
